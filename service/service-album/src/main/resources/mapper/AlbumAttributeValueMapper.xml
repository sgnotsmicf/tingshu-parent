<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.atguigu.tingshu.album.mapper.AlbumAttributeValueMapper">


    <insert id="insertList" parameterType="java.util.List">
        insert into tingshu_album.album_attribute_value(album_id, attribute_id, value_id, create_time, update_time,
        is_deleted) VALUES
        <foreach collection="albumAttributeValueList" item="item" separator=",">
            (#{item.albumId}, #{item.attributeId}, #{item.valueId}, #{item.createTime}, #{item.updateTime},
            #{item.isDeleted})
        </foreach>
    </insert>
</mapper>

